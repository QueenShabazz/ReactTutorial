import React from 'react';

function App() {
  const store = {lists, allCards};
  let lists = [];
  let allCards = {};
  return (
    <div class="App-list">
      <section class="List">
        <header class="List-header">
          <h2>First list</h2>
        </header>
      </section>
    </div>
  );
}

export default App;